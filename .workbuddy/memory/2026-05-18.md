# 2026-05-18

## 搜索引擎配置页面修改
- 修改了 `src/main/resources/static/crawler-settings.html`
- 默认搜索引擎下拉选项从 Google/Bing 改为 Google（需要系统代理）/ 百度
- 同步更新了中英文 i18n 提示文案中的 Google/Bing → Google/Baidu

## 开发信页面动态文案 i18n 修复
- 修改了 `src/main/resources/static/ai-outreach.js`
  - `renderRecipients()` 中用 `innerHTML` 动态生成的空状态文案原来是硬编码中文
  - 新增 `t(key, fallback)` 辅助函数，从 `window.pageTranslations` 按当前 locale 取翻译
  - 空状态「暂无客户」「从客户搜索系统导入，或手动输入」改为动态翻译
  - 客户列表 fallback 文案「待确认」「待人工确认」「未找到公开邮箱」同样改为动态翻译
- 修改了 `src/main/resources/static/ai-outreach.html`
  - 在 `window.pageTranslations` 的 zh-CN 和 en 中补充了 `outreach.status.*` 翻译键

## 开发信页面语言选项 i18n 化
- 修改了 `src/main/resources/static/ai-outreach.html`
  - 语言药丸按钮（.language-pill）文本从原生语言名（English/Deutsch...）统一改为中文（英语/德语...）
  - 语言下拉框（#compose-language）option 文本同样统一改为中文
  - 为药丸和 option 都添加了 `data-i18n` 属性（如 `lang.en`、`lang.de`）
- 修改了 `src/main/resources/static/ai-outreach.js`
  - 在 `localeMessages["zh-CN"]` 和 `localeMessages["en"]` 中补充了 `lang.*` 翻译键值
  - 切换右上角 UI 语言（中文/EN）时，药丸按钮和下拉框选项会自动切换显示语言

## AI 邮件翻译功能
- 修改了 `src/main/resources/static/styles.css`
  - `.ref-editor-top-row` 从 2 列改为 3 列（收件人 | 语言 | 翻译按钮），收件人框横向缩小
- 修改了 `src/main/resources/static/ai-outreach.html`
  - 在语言下拉框右侧添加「翻译邮件」按钮（`#translate-email`）
  - 添加 `outreach.button.translate` i18n 键（中文/英文）
- 修改了 `src/main/resources/static/ai-outreach.js`
  - 添加 `translateButton` DOM 选择器和点击事件绑定
  - 新增 `handleTranslateEmail()` 函数，调用 `POST /api/outreach/translate`
  - 翻译完成后回填主题和正文到输入框
- 修改了后端 Java 代码
  - `WorkflowModels.java`：新增 `TranslateEmailRequest` 和 `TranslateEmailResponse` records
  - `WorkflowDraftService.java`：新增 `translateEmail()` 方法，复用 `aiCompletionService.complete()`
  - `WorkflowService.java`：新增 `translateEmail()` 委托方法
  - `WorkflowApiController.java`：新增 `POST /api/outreach/translate` 端点
- Prompt 设计：System Prompt 指定专业翻译角色，要求保持商务语气、保留专有名词、输出格式为 SUBJECT:/BODY:
- 编译验证通过（`mvn compile`）
